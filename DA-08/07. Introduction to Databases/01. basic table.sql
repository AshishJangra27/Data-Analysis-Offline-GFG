-- create database if not exists dabatch;

-- CREATE TABLE IF NOT EXISTS dabatch.users1 (
--     user_id INT,
--     username VARCHAR(255));
    
    
-- database gfg
-- users (name,phone_no,email,location)

drop database if exists gfgs;

create database gfgs;

create table gfgs.users 
(
	full_name VARCHAR(20),
    phone_no VARCHAR(13),
    email VARCHAR(50),
    location VARCHAR(255)
);


database trends

-- users (user_name, phone_no, email,user_id, dob,location)

-- inventory (product_id, prod_name,quantity,marked_price,discount_perce, 
-- 			dom,dop,doe,prod_desc,size,prod_cat)
--             
-- transection (transection_id, sale_date_time, marked_price, 
-- 			discounted_price ,amount_paid,transection_type)
--             

